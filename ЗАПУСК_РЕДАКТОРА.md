# 🚀 Как запустить визуальный редактор

## Способ 1: Через локальный сервер (РЕКОМЕНДУЕТСЯ)

1. Откройте терминал в папке проекта
2. Запустите сервер:
   ```bash
   node server.js
   ```
   или
   ```bash
   npm run dev
   ```

3. Откройте в браузере:
   ```
   http://localhost:3000/visual-editor.html
   ```

## Способ 2: Простой HTTP сервер

Если `node server.js` не работает, используйте простой сервер:

```bash
npm run dev:simple
```

Затем откройте:
```
http://localhost:3001/visual-editor.html
```

## Способ 3: Прямо открыть файл (может не работать из-за CORS)

Просто откройте файл `visual-editor.html` в браузере двойным кликом.

⚠️ **Внимание**: Если сайт не загружается в iframe, используйте способ 1 или 2.

## Как использовать редактор:

1. **Включите режим редактирования** (чекбокс вверху панели)
2. **Наведите курсор** на любой элемент сайта - увидите пунктирные границы
3. **Кликните** на элемент - он выделится синей рамкой
4. **Перетаскивайте ручки**:
   - 🟢 Зеленая ручка в центре - перемещение элемента
   - 🔵 Синие ручки по углам - изменение размера
   - 🔵 Синие ручки по краям - изменение отступов
5. **Используйте панель управления** для изменения:
   - Шрифтов (семейство, размер, толщина)
   - Цветов (текст, фон)
   - Отступов (верх, низ, слева, справа)

## Если что-то не работает:

1. Откройте консоль браузера (F12)
2. Нажмите кнопку "Тест подключения" в панели управления
3. Проверьте сообщения об ошибках в консоли

## Файлы редактора:

- `visual-editor.html` - главная страница редактора
- `js/visual-editor.js` - логика редактора
- `css/visual-editor.css` - стили интерфейса редактора
