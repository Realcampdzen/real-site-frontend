# 🔍 Инструкция по отладке анимаций

## 🎯 Что нужно проверить:

### ШАГ 1: Жесткая перезагрузка
**КРИТИЧЕСКИ ВАЖНО!**

```
Windows: Ctrl + Shift + R
или: Ctrl + F5
или: Shift + F5
```

Это очистит кэш браузера и загрузит новый код!

---

### ШАГ 2: Откройте консоль
1. Нажмите **F12**
2. Перейдите на вкладку **Console**

---

### ШАГ 3: Проверьте сообщения

Вы должны увидеть:

```
🔮 Проверка Glass UI системы:
GlassUIWidget: function
GlassUIHipych: function
GlassUIBroCat: function
glassUIHipych: object
glassUIBroCat: object
✅ Glass UI система загружена успешно!
🎮 Хипыч готов: [object]
🐱 Кот Бро готов: [object]
🎮 Хипыч кнопка создана с анимациями: glassFloat + pulse-hipych
✅ Кнопка Хипыча найдена
  Animation Хипыча: 4s ease-in-out 0s infinite normal none running glassFloat, 2s ease 0s infinite normal none running pulse-hipych
  Position: fixed 20px 20px
🐱 Кот Бро кнопка создана с анимациями: catFloat + catWiggle + pulse-bro-cat
✅ Кнопка Кота Бро найдена
  Animation Кота: 3s ease-in-out 0s infinite normal none running catFloat, 5s ease-in-out 0s infinite normal none running catWiggle, 2s ease 0s infinite normal none running pulse-bro-cat
  Position: fixed 100px 20px
✅ Карусель инициализирована
  Всего карточек: 8
  Текущий индекс: 0
  Transition карточки: transform 0.8s cubic-bezier(...)
```

---

### ШАГ 4: Проверьте карусель

1. Прокрутите до **"Услуги AI Studio"**
2. Кликните по стрелке **→** (вправо)
3. В консоли должно появиться:
```
🎠 updateCarousel called, animate: true, currentIndex: 1
Card 0: offset=-1, rotateY=-45.0°, translateX=-424.3px
✅ Transition set for card 0
Card 1: offset=0, rotateY=0.0°, translateX=0.0px
✅ Transition set for card 1
Card 2: offset=1, rotateY=45.0°, translateX=424.3px
✅ Transition set for card 2
...
```

---

## 🐛 Возможные проблемы:

### Проблема 1: Анимации НЕ показаны в консоли
```
Animation Хипыча: none
```

**Причина:** Браузер не поддерживает animation в inline стилях  
**Решение:** Проверить что анимации определены в CSS

### Проблема 2: Кнопки не найдены
```
❌ Кнопка Хипыча НЕ найдена!
```

**Причина:** Glass UI скрипты не загрузились  
**Решение:** Проверить пути к файлам

### Проблема 3: Карусель не инициализирована
```
⚠️ window.servicesCarousel is undefined
```

**Причина:** services-carousel.js не загрузился  
**Решение:** Проверить путь к файлу

### Проблема 4: Transition = "none"
```
Transition карточки: none
```

**Причина:** JS не применяет transition  
**Решение:** Кликнуть по стрелке и проверить консоль

---

## 🔧 Ручная проверка анимаций:

### В консоли выполните:

```javascript
// Проверка анимаций ботов
const hipychBtn = document.getElementById('glass-ui-hipych-button');
console.log('Хипыч animation:', hipychBtn?.style.animation);

const broCatBtn = document.getElementById('glass-ui-bro-cat-button');
console.log('Кот animation:', broCatBtn?.style.animation);

// Проверка transitions карусели
const cards = document.querySelectorAll('.service-carousel-card');
cards.forEach((card, i) => {
  console.log(`Card ${i} transition:`, card.style.transition);
  console.log(`Card ${i} transform:`, card.style.transform);
});
```

---

## 📸 Скриншот консоли

Пожалуйста, **сделайте скриншот** консоли (F12 → Console) и покажите мне что там выводится!

Это поможет понять где именно проблема.

---

## ⚡ Быстрое исправление

Если ничего не помогает, выполните в консоли:

```javascript
// Принудительная инициализация
location.reload(true); // Полная перезагрузка

// Или очистка кэша:
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.getRegistrations().then(registrations => {
    registrations.forEach(registration => registration.unregister());
  });
}
```

---

**Следующий шаг:** Жесткая перезагрузка (Ctrl+Shift+R) → F12 → Скриншот консоли → Показать мне!

