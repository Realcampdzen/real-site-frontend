# 🔄 Процесс разработки и деплоя

## 📋 Двухуровневая система деплоя

### 🧪 GitHub Pages (Staging) - Полная версия для тестирования
- **URL**: `https://realcampdzen.github.io/Real_Vibe_AI_Studio_New/`
- **Назначение**: Тестирование всех функций, включая недоработанные
- **Включает**:
  - ✅ Все тестовые функции
  - ✅ Недоработанные разделы
  - ✅ Боты без подключения к AI (для тестирования UI)
  - ✅ Placeholder изображения
  - ✅ Все функции в разработке
- **Деплой**: Автоматический при push в `main` ветку

### 🚀 NIC.RU (Production) - Готовая версия
- **URL**: `https://real-vibe.studio/`
- **Назначение**: Только готовые функции для клиентов
- **Включает**:
  - ✅ Только завершённые функции
  - ✅ Боты с подключением к AI
  - ✅ Все изображения на месте
  - ✅ Все разделы доделаны
  - ❌ Без тестовых функций
  - ❌ Без placeholder элементов
- **Деплой**: Ручной через `prepare-deploy.ps1`

---

## 🔄 Ежедневный workflow

### 1. Разработка локально

```bash
# Тестируете изменения локально
npm run dev
# или
npm run dev:simple
```

### 2. Деплой на GitHub Pages (Staging) - Автоматически

После того как вы готовы протестировать изменения:

```bash
# Добавляете изменения
git add .

# Коммитите с описанием
git commit -m "Добавлена новая функция X"

# Пушите в репозиторий
git push origin main
```

**Что происходит:**
- ✅ GitHub Actions автоматически запускается
- ✅ Деплоит полную версию на GitHub Pages
- ✅ Занимает ~2-3 минуты
- ✅ Можно проверить на staging URL

**Проверка:**
- Откройте: `https://realcampdzen.github.io/Real_Vibe_AI_Studio_New/`
- Проверьте что всё работает
- Если есть проблемы - исправляете локально и пушите снова

### 3. Деплой на NIC.RU (Production) - Ручной

Когда всё готово и протестировано на GitHub Pages:

#### Шаг 1: Подготовка production версии

```powershell
# Запускаете скрипт подготовки
.\prepare-deploy.ps1
```

**Что делает скрипт:**
- ✅ Создаёт папку `deploy-ready/`
- ✅ Копирует все необходимые файлы
- ✅ Удаляет dev файлы (node_modules, логи и т.д.)
- ✅ Удаляет debug/test файлы
- ✅ Готовит чистую production версию

#### Шаг 2: Получить список файлов для загрузки

```powershell
# Создаст список всех файлов с указанием куда загружать
.\list-files-to-upload.ps1
```

Этот скрипт покажет:
- ✅ Все файлы, которые нужно загрузить
- ✅ В каких папках они находятся
- ✅ Пути для загрузки на NIC.RU

#### Шаг 3: Загрузка на NIC.RU (важно!)

**⚠️ Важно:** На NIC.RU нельзя загружать целые папки. Нужно загружать файлы **по одному в каждую папку**.

1. Откройте панель NIC.RU: https://www.nic.ru/hcp2/
2. Перейдите в **"Файловый менеджер"**
3. Откройте папку `docs/` (корневой каталог сайта)

4. **Загрузка по папкам:**
   - **Корневые файлы** (`index.html`, `manifest.json` и т.д.):
     - Откройте папку `docs/` на сервере
     - Загрузите файлы из `deploy-ready/` (не из подпапок!)
   
   - **Файлы в подпапках:**
     - Откройте нужную папку на сервере (например, `docs/css/`)
     - Загрузите файлы из соответствующей папки в `deploy-ready/` (например, `deploy-ready/css/`)
     - Повторите для каждой папки: `js/`, `public/`, `images/`, `img/`, `chat-components/`

5. **Для подпапок внутри папок** (например, `public/works/`):
   - Создайте папку на сервере, если её нет
   - Откройте её и загрузите файлы из `deploy-ready/public/works/`

#### Шаг 3: Проверка

1. Подождите 1-2 минуты
2. Откройте: https://real-vibe.studio/
3. Проверьте что всё работает
4. Проверьте консоль браузера (F12) на ошибки

---

## 📝 Что включить/исключить

### Для GitHub Pages (Staging) - Всё включено

- ✅ Все файлы из репозитория
- ✅ Все тестовые функции
- ✅ Недоработанные разделы
- ✅ Placeholder изображения
- ❌ Только dev файлы исключены (node_modules, логи и т.д.)

### Для NIC.RU (Production) - Только готовое

- ✅ Готовые функции
- ✅ Завершённые разделы
- ✅ Все изображения на месте
- ✅ Боты с AI подключением
- ❌ Тестовые функции
- ❌ Недоработанные разделы
- ❌ Placeholder элементы

**Примечание:** Логику исключения тестовых функций можно добавить в `prepare-deploy.ps1` когда будете готовы.

---

## 🔧 Настройка исключения тестовых функций

Когда будете готовы исключать тестовые функции из production, откройте `prepare-deploy.ps1` и добавьте логику в раздел:

```powershell
# PRODUCTION: Remove test features
# TODO: Add logic here to remove/modify test features when ready
```

**Примеры что можно исключить:**
- Удаление HTML блоков с недоработанными разделами
- Отключение тестовых ботов в JS файлах
- Удаление placeholder изображений
- Модификация конфигурации для production

---

## 📊 Чек-лист перед деплоем на Production

- [ ] Протестировано на GitHub Pages (staging)
- [ ] Все функции работают корректно
- [ ] Нет ошибок в консоли браузера
- [ ] Все изображения загружаются
- [ ] Все видео работают
- [ ] Мобильная версия проверена
- [ ] Запущен `prepare-deploy.ps1`
- [ ] Проверена папка `deploy-ready/`
- [ ] Загружено на NIC.RU
- [ ] Проверено на https://real-vibe.studio/

---

## 🆘 Решение проблем

### GitHub Pages не обновляется

1. Проверьте статус в **Actions** на GitHub
2. Убедитесь что коммит запушен в ветку `main`
3. Подождите 2-3 минуты

### NIC.RU показывает старую версию

1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Проверьте что файлы загружены в папку `docs/`
3. Проверьте структуру папок на сервере

### Файлы не загружаются

1. Проверьте размер файлов (видео могут быть большими)
2. Проверьте права доступа на сервере
3. Попробуйте загрузить файлы по частям

---

## 📚 Полезные команды

```bash
# Локальный запуск для тестирования
npm run dev

# Простой HTTP сервер
npm run dev:simple

# Git команды
git status                    # Проверить изменения
git add .                     # Добавить все изменения
git commit -m "Описание"      # Закоммитить
git push origin main          # Отправить на GitHub
```

---

## 🎯 Итоговая схема

```
Локальная разработка
    ↓
git push → GitHub Pages (Staging) [Автоматически]
    ↓
Тестирование на staging
    ↓
prepare-deploy.ps1 → deploy-ready/
    ↓
Загрузка на NIC.RU → Production [Ручной]
```

---

**Теперь у вас есть полноценная система для разработки и деплоя!** 🚀

