# 📤 Инструкция по загрузке файлов на NIC.RU

## ⚠️ Важная особенность NIC.RU

**На NIC.RU нельзя загружать целые папки!**  
Нужно загружать файлы **по одному в каждую существующую папку** на сервере.

---

## 📋 Процесс загрузки

### Шаг 1: Подготовка файлов

```powershell
# Подготовить production версию
.\prepare-deploy.ps1

# Получить список файлов для загрузки
.\list-files-to-upload.ps1
```

### Шаг 2: Загрузка на сервер

1. Откройте панель NIC.RU: https://www.nic.ru/hcp2/
2. Перейдите в **"Файловый менеджер"**
3. Откройте папку `docs/` (корневой каталог сайта)

### Шаг 3: Загрузка по папкам

#### Корневые файлы (в папке `docs/`):

В папке `docs/` на сервере загрузите файлы из `deploy-ready/`:

- `index.html`
- `manifest.json`
- `sw.js`
- `service-detail.html`
- `ai-photo-detail.html`
- `.htaccess`

**Как загрузить:**
1. Убедитесь что вы в папке `docs/` на сервере
2. Нажмите "Загрузить" или перетащите файлы
3. Выберите файлы из `deploy-ready/` (не из подпапок!)

---

#### Файлы в папке `css/`:

1. Откройте папку `docs/css/` на сервере (или создайте, если её нет)
2. Загрузите файлы из `deploy-ready/css/`:
   - `style.css`
   - `critical-fixes.css`
   - `mobile-improvements.css`
   - и т.д.

---

#### Файлы в папке `js/`:

1. Откройте папку `docs/js/` на сервере
2. Загрузите файлы из `deploy-ready/js/`:
   - `script.js`
   - `hero-reveal.js`
   - `chat.js`
   - и т.д.

---

#### Файлы в папке `public/`:

1. Откройте папку `docs/public/` на сервере
2. Загрузите файлы из `deploy-ready/public/`:
   - `logo-mark.svg`
   - `работа.jpg`
   - и другие файлы из корня `public/`

**Для подпапки `public/works/`:**
1. Откройте или создайте папку `docs/public/works/` на сервере
2. Загрузите файлы из `deploy-ready/public/works/`:
   - Все изображения `.jpg`
   - Все видео `.mp4`
   - И другие файлы

---

#### Файлы в папке `images/`:

1. Откройте папку `docs/images/` на сервере
2. Загрузите файлы из `deploy-ready/images/`:
   - `bro-avatar.jpg`
   - `hipych-avatar.jpg`
   - `neon-room.png`

---

#### Файлы в папке `img/`:

1. Откройте папку `docs/img/` на сервере
2. Загрузите файлы из `deploy-ready/img/`:
   - Все фоновые изображения

---

#### Файлы в папке `chat-components/`:

1. Откройте папку `docs/chat-components/` на сервере
2. Загрузите файлы из `deploy-ready/chat-components/`:
   - `GlassUIWidget.js`
   - `ModernChatWidget.js`
   - `SimpleChatWidget.js`

---

## 💡 Советы

### Обновление только изменённых файлов

Если вы обновляете только несколько файлов:

1. Запустите `.\prepare-deploy.ps1` (чтобы обновить `deploy-ready/`)
2. Определите какие файлы изменились
3. Загрузите только изменённые файлы в соответствующие папки на сервере

**Пример:**
- Изменили `index.html` → загрузите только его в `docs/`
- Изменили `css/style.css` → загрузите только его в `docs/css/`
- Добавили новое изображение → загрузите его в соответствующую папку

---

### Создание новых папок

Если на сервере нет нужной папки:

1. В файловом менеджере нажмите "Создать папку" или "Новая папка"
2. Введите имя папки (например, `works` внутри `public/`)
3. Откройте созданную папку
4. Загрузите файлы

---

### Проверка структуры

После загрузки проверьте структуру на сервере:

```
docs/
├── index.html
├── manifest.json
├── sw.js
├── css/
│   └── *.css
├── js/
│   └── *.js
├── public/
│   ├── *.svg, *.jpg
│   └── works/
│       └── *.jpg, *.mp4
├── images/
│   └── *.jpg, *.png
├── img/
│   └── *.png, *.jpg
└── chat-components/
    └── *.js
```

---

## ⚡ Быстрая загрузка

Если вы обновили только один файл:

1. Откройте нужную папку на сервере
2. Нажмите "Загрузить"
3. Выберите файл из `deploy-ready/`
4. Замените старый файл

**Готово!** Не нужно загружать всё заново.

---

**Подробная инструкция по процессу:** См. `DEVELOPMENT_WORKFLOW.md`

